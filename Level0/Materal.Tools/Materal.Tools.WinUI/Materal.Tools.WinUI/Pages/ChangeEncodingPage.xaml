<Page
    x:Class="Materal.Tools.WinUI.Pages.ChageEncodingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Materal.Tools.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <StackPanel Spacing="10">
        <StackPanel Orientation="Horizontal" Spacing="5">
            <TextBox Header="路径" Text="{x:Bind ViewModel.Path, Mode=TwoWay}" CornerRadius="4,0,0,4" MinWidth="100" />
            <TextBox Header="文件名正则" Text="{x:Bind ViewModel.FileNameFilter, Mode=TwoWay}" />
            <TextBox Header="字符集" Text="{x:Bind ViewModel.EncodingName, Mode=TwoWay}" />
            <ToggleButton VerticalAlignment="Bottom" IsChecked="{x:Bind ViewModel.Recursive, Mode=TwoWay}" Content="递归转换" />
            <ToggleButton VerticalAlignment="Bottom" IsChecked="{x:Bind ViewModel.ForcedRead, Mode=TwoWay}" Content="强制读取" />
            <TextBox Header="读取字符集" Text="{x:Bind ViewModel.ReadEncodingName, Mode=TwoWay}" />
        </StackPanel>
        <Button Content="开始转换" Width="200" Command="{x:Bind ViewModel.ChangeEncodingCommand}" />
        <ScrollViewer Height="560">
            <ItemsControl ItemsSource="{x:Bind ViewModel.LogMessages, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <TextBlock IsTextSelectionEnabled="True" Text="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </StackPanel>
</Page>
