<UserControl
    x:Class="RCManagementTool.Controls.Paging"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RCManagementTool.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <ItemsPanelTemplate x:Key="IndexButtonGroupTemplate">
            <StackPanel Orientation="Horizontal" Spacing="5" />
        </ItemsPanelTemplate>
        <DataTemplate x:Key="IndexButtonTemplate" x:DataType="local:PagingButtonModel">
            <ToggleButton Content="{Binding Index, Mode=OneWay}" IsChecked="{Binding IsChecked, Mode=OneWay}" Command="{Binding ChangeIndexCommand}" />
        </DataTemplate>
    </UserControl.Resources>

    <StackPanel Orientation="Horizontal" Spacing="5">
        <Button Content="&lt;" Command="{x:Bind ViewModel.UpPageCommand}" />
        <ItemsControl ItemsSource="{x:Bind ViewModel.PageIndexList, Mode=OneWay}" ItemTemplate="{StaticResource IndexButtonTemplate}" ItemsPanel="{StaticResource IndexButtonGroupTemplate}" />
        <Button Content="&gt;" Command="{x:Bind ViewModel.NextPageCommand}"/>
        <ComboBox ItemsSource="{x:Bind ViewModel.PageSizeList}" SelectedItem="{x:Bind ViewModel.PageSize, Mode=TwoWay}" />
    </StackPanel>
</UserControl>
