<Page
    x:Class="RCManagementTool.Pages.Authority.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RCManagementTool.Pages.Authority"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <local:LoginPageViewModel x:Name="ViewModel" />
    </Page.DataContext>

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="10" Width="400" 
                Background="{StaticResource CardStrokeColorDefaultBrush}" Padding="20" CornerRadius="4">
        <TextBlock FontSize="24" HorizontalAlignment="Stretch" TextAlignment="Center" Text="RC管理工具" />
        <TextBox Header="账号" Text="{x:Bind ViewModel.Account, Mode=TwoWay}">
            <TextBox.Description>
                <TextBlock Foreground="{StaticResource SystemErrorTextColor}" Text="{x:Bind ViewModel.AccountErrorMessage, Mode=OneWay}" />
            </TextBox.Description>
        </TextBox>
        <PasswordBox Header="密码" PasswordChar="*" Password="{x:Bind ViewModel.Password, Mode=TwoWay}">
            <PasswordBox.Description>
                <TextBlock Foreground="{StaticResource SystemErrorTextColor}" Text="{x:Bind ViewModel.PasswordErrorMessage, Mode=OneWay}" />
            </PasswordBox.Description>
        </PasswordBox>
        <Button HorizontalAlignment="Stretch" Content="登录" Command="{x:Bind ViewModel.LoginCommand}" />
    </StackPanel>
</Page>
