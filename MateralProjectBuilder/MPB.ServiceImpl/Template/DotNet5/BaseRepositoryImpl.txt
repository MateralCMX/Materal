using Materal.TTA.Common;
using Materal.TTA.SqlServerRepository;

namespace ${ProjectName}.SqlServerEFRepository
{
    /// <summary>
    /// ${ProjectName}仓储实现
    /// </summary>
    public abstract class ${ProjectName}EFRepositoryImpl<T, TKey> : SqlServerEFRepositoryImpl<T, TKey> where T : class, IEntity<TKey>, new()
    {
        public ${ProjectName}EFRepositoryImpl(${ProjectName}DbContext dbContext) : base(dbContext)
        {
        }
    }
}
