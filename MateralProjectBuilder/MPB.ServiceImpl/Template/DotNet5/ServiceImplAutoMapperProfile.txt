using AutoMapper;
using ${ProjectName}.DataTransmitModel.${RelativeNamespace}.${Name};
using ${ProjectName}.Domain.${RelativeNamespace};

namespace ${ProjectName}.ServiceImpl.${RelativeNamespace}.AutoMapperProfile
{
    /// <summary>
    /// ${Annotation}AutoMapper配置
    /// </summary>
    public sealed class ${Name}Profile : Profile
    {
        public ${Name}Profile()
        {
            CreateMap<${Name}, ${Name}ListDTO>();
            CreateMap<${Name}, ${Name}DTO>();
        }
    }
}
